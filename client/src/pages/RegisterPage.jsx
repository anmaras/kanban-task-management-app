import React from 'react';
import { Form, AuthLayout } from '../components';

const RegisterPage = () => {
  return (
    <AuthLayout>
      <Form type="register" />
    </AuthLayout>
  );
};

export default RegisterPage;
